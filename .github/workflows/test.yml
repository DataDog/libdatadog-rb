name: Test
on:
  workflow_call:
  workflow_dispatch:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os: linux
            cpu: x86_64
            base: ubuntu-24.04 # always x86_64-linux-gnu
          - os: linux
            cpu: aarch64
            base: ubuntu-24.04-arm # always aarch64-linux-gnu
          - os: darwin
            cpu: arm64
            base: macos-15 # always arm64-darwin
        ruby:
          - version: "2.5"
            bundler: "2.3.27"
          - version: "2.6"
            bundler: "2.4.22"
          - version: "2.7"
            bundler: "2.4.22"
          - version: "3.0"
            bundler: "2.5.23"
          - version: "3.1"
            bundler: "2.6.9"
          - version: "3.2"
            bundler: "4.0.3"
          - version: "3.3"
            bundler: "4.0.3"
          - version: "3.4"
            bundler: "4.0.3"
          - version: "4.0"
            bundler: "4.0.3"
        exclude:
          # Ruby 2.5 is not available on arm64-darwin
          - platform:
              os: darwin
            ruby:
              version: "2.5"
    name: Ruby ${{ matrix.ruby.version }} (${{ matrix.platform.cpu }}-${{ matrix.platform.os }})
    runs-on: ${{ matrix.platform.base }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # 7.0.0
      - uses: ruby/setup-ruby@9eb537ca036ebaed86729dcb9309076e4c5c3b74 # 1.314.0
        with:
          ruby-version: ${{ matrix.ruby.version }}
          bundler: ${{ matrix.ruby.bundler }}
          bundler-cache: true # runs 'bundle install' and caches installed gems automatically
      - run: bundle exec rake
